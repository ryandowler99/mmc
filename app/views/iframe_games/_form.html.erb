<%= form_for(@iframe_game) do |f| %>
  <% if @iframe_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@iframe_game.errors.count, "error") %> prohibited this iframe_game from being saved:</h2>

      <ul>
      <% @iframe_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
